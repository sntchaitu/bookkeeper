{% extends '::base.html.twig' %}
{% block body %}
<h1>Book #{{ book.id }}-{{ book.title }}</h1>
    <h3>No. of Pages: {{ book.pages }}</h3>
    <p>{{ book.description }}</p>
    <ul>
        <l1><a href="{{ path('book') }}">Book Manager</a></l1>
        <l1><a href="{{ path('book_edit',{'id':book.id}) }}">Edit Book</a></l1>
        <l1>{{ form(delete_form) }}</l1>
    </ul>
{% endblock %}
